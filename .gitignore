# ── Python ──
__pycache__/
*.pyc
*.pyo

# ── Environments ──
.venv/
.venv_tenseal/

# ── Editor ──
.vscode/
.DS_Store

# ── Early experiments — not part of project ──
experiment/

# ── Generated keys — never commit ──
*.tenseal
vault_med/client_and_server/shared/

# ── Image dataset — download from Kaggle ──
vault_med/feature_extraction/image_dataset/

# ── Logs — server generated ──
vault_med/client_and_server/logs/

# ── Personal notes ──
Note.py

# ── Local Flask middleware ──
vault_med/client_and_server/client/app.py

# ── Extracted features — generated by feature_extraction.py ──
vault_med/feature_extraction/data/train_features.npy
vault_med/feature_extraction/data/test_features.npy
vault_med/feature_extraction/data/train_labels.npy
vault_med/feature_extraction/data/test_labels.npy